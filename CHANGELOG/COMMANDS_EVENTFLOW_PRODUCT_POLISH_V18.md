# Komendy — EventFlow Product Polish V18

Pobierz `EventFlow_Product_Polish_v18_patch.zip` do `~/Downloads`, potem uruchom standardowy zestaw komend z backupem i `rsync`.

V18 nie wymaga migracji bazy, ale można bezpiecznie uruchomić `prisma generate`.
